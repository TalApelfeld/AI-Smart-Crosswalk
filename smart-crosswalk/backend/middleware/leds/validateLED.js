/**
 * Placeholder middleware for LED validation.
 * LED model has no required fields, but this file is included
 * for consistency with the domain middleware structure.
 */
export function validateCreateLED(req, res, next) {
  next();
}
